# Build-time env for production static frontend (Vite embeds these at `vite build`).
# Changing this on the host AFTER build does nothing — rebuild & redeploy the frontend.
# Must include /api — must NOT be http://localhost or http://127.0.0.1
VITE_API_URL=https://api.krishnainfotec.com/api
