# Build-time env for production static frontend (Vite embeds these at build time).
# Changing this on the host AFTER build does nothing — rebuild & redeploy the frontend.
# Must include /api — must NOT be http://localhost:3001
VITE_API_URL=https://api.krishnainfotec.com/api
