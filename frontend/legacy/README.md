# Legacy UI prototype

`battery.jsx` is an early **standalone** inventory UI (mock data, dark theme, no API).

The live React app is under `../src/` (Vite + routed pages).

Do **not** import this into production routes unless you intentionally rebuild it against `../src/api/client.js`.
