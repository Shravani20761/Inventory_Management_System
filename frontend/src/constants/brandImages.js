/** Client re-export of shared image mapping (paths match dev server & production `/images/...`). */
export * from "../../shared/productImages.js";

/** Compact badge colors for inline Smart Quotation styles (separate from server PDF BADGE_COLORS). */
export const BADGE_COLORS = {
  Budget: "#059669",
  Recommended: "#2563eb",
  Premium: "#6B21D8",
  "Heavy Load": "#ea580c",
};
